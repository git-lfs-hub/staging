{
  "name": "@git-lfs-hub/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e-test": "vitest run --reporter=verbose --reporter=github-actions",
    "e2e-test:watch": "vitest watch"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
