{
  "name": "@git-lfs-hub/staging",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=github-actions",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
