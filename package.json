{
  "name": "@git-lfs-hub/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e-test": "vitest run --reporter=verbose --reporter=github-actions",
    "e2e-test:watch": "vitest watch"
  },
  "dependencies": {
    "@git-lfs-hub/lib": "file:../lib"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "vitest": "^4.1.7"
  }
}
